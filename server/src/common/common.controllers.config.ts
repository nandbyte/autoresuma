import { Request,Response } from "express";
import {Json} from "sequelize/types/lib/utils";
import {v4 as uuidv4 } from 'uuid';

export abstract class CommonControllerConfig{
//	abstract create(req:Express.Request,res: Express.Response ): Promise<Json>;
//	abstract readPagination( req:Express.Request,res: Express.Response  ):Promise<Json>;
//	abstract readById(req:Express.Request,res: Express.Response ):Promise<Json>;
//	abstract update(req:Express.Request,res: Express.Response):Promise<Json>;
//	abstract delete(req:Express.Request,res: Express.Response):Promise<Json>;
}
